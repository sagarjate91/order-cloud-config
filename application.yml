eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost

microservice:
     payment-service:
              endpoint: http://PAYMENT-SERVICE/pay/savePayment
spring:
      datasource:
             url: jdbc:mysql://sagarjate.chekqcoms99r.ap-south-1.rds.amazonaws.com:3306/flipkart
             username: root
             password: sagarjate91
      jpa:
        hibernate:
             ddl-auto: update
              
    
